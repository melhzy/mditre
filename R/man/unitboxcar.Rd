% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math_utils.R
\name{unitboxcar}
\alias{unitboxcar}
\title{Unit Height Boxcar Function}
\usage{
unitboxcar(x, mu, l, k)
}
\arguments{
\item{x}{Input tensor (typically time points)}

\item{mu}{Center position of the boxcar}

\item{l}{Length of the boxcar}

\item{k}{Sharpness parameter (higher = sharper edges)}
}
\value{
Tensor with boxcar function values
}
\description{
Approximate a unit height boxcar (rectangular) function using analytic
approximations of the Heaviside function. Used for soft temporal windowing.
}
\details{
The boxcar is parameterized by its center (mu) and length (l).
The function smoothly transitions from 0 to 1 at the edges.
}
\examples{
\dontrun{
library(torch)
# Create time points
x <- torch_linspace(0, 1, 100)
# Boxcar centered at 0.5 with length 0.2
y <- unitboxcar(x, mu = 0.5, l = 0.2, k = 10)
plot(as_array(x), as_array(y), type = "l")
}
}
