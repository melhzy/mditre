% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phyloseq_loader.R
\name{create_dataloader}
\alias{create_dataloader}
\title{Create torch DataLoader for MDITRE Data}
\usage{
create_dataloader(mditre_data, batch_size = 32, shuffle = TRUE)
}
\arguments{
\item{mditre_data}{List returned from \code{phyloseq_to_mditre}}

\item{batch_size}{Integer. Batch size for training (default: 32)}

\item{shuffle}{Logical. Whether to shuffle data (default: TRUE)}
}
\value{
torch dataloader object
}
\description{
Wraps MDITRE-formatted data in a torch dataset and dataloader.
}
\examples{
\dontrun{
# Create dataloader
train_loader <- create_dataloader(
  split_data$train,
  batch_size = 32,
  shuffle = TRUE
)

# Iterate through batches
coro::loop(for (batch in train_loader) {
  X_batch <- batch[[1]]
  y_batch <- batch[[2]]
  # Training step...
})
}
}
