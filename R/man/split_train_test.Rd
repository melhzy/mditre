% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phyloseq_loader.R
\name{split_train_test}
\alias{split_train_test}
\title{Split Data into Train and Test Sets}
\usage{
split_train_test(
  mditre_data,
  test_fraction = 0.2,
  stratify = TRUE,
  seed = NULL
)
}
\arguments{
\item{mditre_data}{List returned from \code{phyloseq_to_mditre}}

\item{test_fraction}{Numeric. Fraction of subjects to use for testing (default: 0.2)}

\item{stratify}{Logical. Whether to stratify split by labels (default: TRUE)}

\item{seed}{Integer. Random seed for reproducibility (default: NULL)}
}
\value{
List with \code{train} and \code{test} components, each containing
the same structure as input \code{mditre_data}
}
\description{
Splits MDITRE-formatted data into training and testing sets.
}
\examples{
\dontrun{
# Split data
split_data <- split_train_test(mditre_data, test_fraction = 0.2, seed = 42)

# Access train/test sets
X_train <- split_data$train$X
X_test <- split_data$test$X
}
}
