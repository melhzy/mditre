% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{cross_validate_mditre}
\alias{cross_validate_mditre}
\title{K-Fold Cross-Validation}
\usage{
cross_validate_mditre(
  mditre_data,
  k = 5,
  model_params,
  train_params = list(),
  stratified = TRUE,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{mditre_data}{Full MDITRE data object (from phyloseq_to_mditre)}

\item{k}{Number of folds (default: 5)}

\item{model_params}{Named list of model parameters (num_rules, num_otus, num_time, dist)}

\item{train_params}{Named list of training parameters (epochs, learning_rates, etc.)}

\item{stratified}{Whether to use stratified splitting (default: TRUE)}

\item{seed}{Random seed for reproducibility (default: 42)}

\item{verbose}{Print progress (default: TRUE)}
}
\value{
List with:
\itemize{
\item fold_results: List of results for each fold
\item mean_metrics: Average metrics across folds
\item std_metrics: Standard deviation of metrics
\item all_predictions: All predictions (if requested)
\item all_labels: All true labels (if requested)
}
}
\description{
Perform k-fold cross-validation for MDITRE model training and evaluation.
}
\examples{
\dontrun{
# Prepare data
mditre_data <- phyloseq_to_mditre(ps, "Subject", "Time", "Disease")

# Model parameters
model_params <- list(
  num_rules = 5,
  num_otus = mditre_data$metadata$n_otus,
  num_time = mditre_data$metadata$n_timepoints,
  dist = mditre_data$phylo_dist
)

# Training parameters
train_params <- list(epochs = 200, early_stopping_patience = 20)

# Run cross-validation
cv_results <- cross_validate_mditre(
  mditre_data,
  k = 5,
  model_params = model_params,
  train_params = train_params
)

# Print results
print(cv_results$mean_metrics)
}

}
